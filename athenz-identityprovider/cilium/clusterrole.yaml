apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: identityprovider-remote-reader
rules:
  - apiGroups:
      - ""
    resources:
      - pods
    verbs:
      - list
      - watch
  - apiGroups:
      - ""
    resources:
      - nodes
    verbs:
      - list
